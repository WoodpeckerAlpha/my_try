import { FC } from "react";

import { IList } from "../../../models/ListsModel/IList";

const ListPlate: FC<IList> = ({ title, textField }) => {
    return <h1></h1>;
};

export default ListPlate;
